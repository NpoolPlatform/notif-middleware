// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/notif-middleware/pkg/db/ent/predicate"
	"github.com/NpoolPlatform/notif-middleware/pkg/db/ent/usernotif"
	"github.com/google/uuid"
)

// UserNotifQuery is the builder for querying UserNotif entities.
type UserNotifQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.UserNotif
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UserNotifQuery builder.
func (unq *UserNotifQuery) Where(ps ...predicate.UserNotif) *UserNotifQuery {
	unq.predicates = append(unq.predicates, ps...)
	return unq
}

// Limit adds a limit step to the query.
func (unq *UserNotifQuery) Limit(limit int) *UserNotifQuery {
	unq.limit = &limit
	return unq
}

// Offset adds an offset step to the query.
func (unq *UserNotifQuery) Offset(offset int) *UserNotifQuery {
	unq.offset = &offset
	return unq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (unq *UserNotifQuery) Unique(unique bool) *UserNotifQuery {
	unq.unique = &unique
	return unq
}

// Order adds an order step to the query.
func (unq *UserNotifQuery) Order(o ...OrderFunc) *UserNotifQuery {
	unq.order = append(unq.order, o...)
	return unq
}

// First returns the first UserNotif entity from the query.
// Returns a *NotFoundError when no UserNotif was found.
func (unq *UserNotifQuery) First(ctx context.Context) (*UserNotif, error) {
	nodes, err := unq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{usernotif.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (unq *UserNotifQuery) FirstX(ctx context.Context) *UserNotif {
	node, err := unq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UserNotif ID from the query.
// Returns a *NotFoundError when no UserNotif ID was found.
func (unq *UserNotifQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = unq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{usernotif.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (unq *UserNotifQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := unq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UserNotif entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UserNotif entity is found.
// Returns a *NotFoundError when no UserNotif entities are found.
func (unq *UserNotifQuery) Only(ctx context.Context) (*UserNotif, error) {
	nodes, err := unq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{usernotif.Label}
	default:
		return nil, &NotSingularError{usernotif.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (unq *UserNotifQuery) OnlyX(ctx context.Context) *UserNotif {
	node, err := unq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UserNotif ID in the query.
// Returns a *NotSingularError when more than one UserNotif ID is found.
// Returns a *NotFoundError when no entities are found.
func (unq *UserNotifQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = unq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{usernotif.Label}
	default:
		err = &NotSingularError{usernotif.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (unq *UserNotifQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := unq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UserNotifs.
func (unq *UserNotifQuery) All(ctx context.Context) ([]*UserNotif, error) {
	if err := unq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return unq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (unq *UserNotifQuery) AllX(ctx context.Context) []*UserNotif {
	nodes, err := unq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UserNotif IDs.
func (unq *UserNotifQuery) IDs(ctx context.Context) ([]uuid.UUID, error) {
	var ids []uuid.UUID
	if err := unq.Select(usernotif.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (unq *UserNotifQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := unq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (unq *UserNotifQuery) Count(ctx context.Context) (int, error) {
	if err := unq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return unq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (unq *UserNotifQuery) CountX(ctx context.Context) int {
	count, err := unq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (unq *UserNotifQuery) Exist(ctx context.Context) (bool, error) {
	if err := unq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return unq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (unq *UserNotifQuery) ExistX(ctx context.Context) bool {
	exist, err := unq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UserNotifQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (unq *UserNotifQuery) Clone() *UserNotifQuery {
	if unq == nil {
		return nil
	}
	return &UserNotifQuery{
		config:     unq.config,
		limit:      unq.limit,
		offset:     unq.offset,
		order:      append([]OrderFunc{}, unq.order...),
		predicates: append([]predicate.UserNotif{}, unq.predicates...),
		// clone intermediate query.
		sql:    unq.sql.Clone(),
		path:   unq.path,
		unique: unq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UserNotif.Query().
//		GroupBy(usernotif.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (unq *UserNotifQuery) GroupBy(field string, fields ...string) *UserNotifGroupBy {
	grbuild := &UserNotifGroupBy{config: unq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := unq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return unq.sqlQuery(ctx), nil
	}
	grbuild.label = usernotif.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.UserNotif.Query().
//		Select(usernotif.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (unq *UserNotifQuery) Select(fields ...string) *UserNotifSelect {
	unq.fields = append(unq.fields, fields...)
	selbuild := &UserNotifSelect{UserNotifQuery: unq}
	selbuild.label = usernotif.Label
	selbuild.flds, selbuild.scan = &unq.fields, selbuild.Scan
	return selbuild
}

func (unq *UserNotifQuery) prepareQuery(ctx context.Context) error {
	for _, f := range unq.fields {
		if !usernotif.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if unq.path != nil {
		prev, err := unq.path(ctx)
		if err != nil {
			return err
		}
		unq.sql = prev
	}
	if usernotif.Policy == nil {
		return errors.New("ent: uninitialized usernotif.Policy (forgotten import ent/runtime?)")
	}
	if err := usernotif.Policy.EvalQuery(ctx, unq); err != nil {
		return err
	}
	return nil
}

func (unq *UserNotifQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UserNotif, error) {
	var (
		nodes = []*UserNotif{}
		_spec = unq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*UserNotif).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &UserNotif{config: unq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(unq.modifiers) > 0 {
		_spec.Modifiers = unq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, unq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (unq *UserNotifQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := unq.querySpec()
	if len(unq.modifiers) > 0 {
		_spec.Modifiers = unq.modifiers
	}
	_spec.Node.Columns = unq.fields
	if len(unq.fields) > 0 {
		_spec.Unique = unq.unique != nil && *unq.unique
	}
	return sqlgraph.CountNodes(ctx, unq.driver, _spec)
}

func (unq *UserNotifQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := unq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (unq *UserNotifQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   usernotif.Table,
			Columns: usernotif.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUUID,
				Column: usernotif.FieldID,
			},
		},
		From:   unq.sql,
		Unique: true,
	}
	if unique := unq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := unq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, usernotif.FieldID)
		for i := range fields {
			if fields[i] != usernotif.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := unq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := unq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := unq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := unq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (unq *UserNotifQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(unq.driver.Dialect())
	t1 := builder.Table(usernotif.Table)
	columns := unq.fields
	if len(columns) == 0 {
		columns = usernotif.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if unq.sql != nil {
		selector = unq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if unq.unique != nil && *unq.unique {
		selector.Distinct()
	}
	for _, m := range unq.modifiers {
		m(selector)
	}
	for _, p := range unq.predicates {
		p(selector)
	}
	for _, p := range unq.order {
		p(selector)
	}
	if offset := unq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := unq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (unq *UserNotifQuery) ForUpdate(opts ...sql.LockOption) *UserNotifQuery {
	if unq.driver.Dialect() == dialect.Postgres {
		unq.Unique(false)
	}
	unq.modifiers = append(unq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return unq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (unq *UserNotifQuery) ForShare(opts ...sql.LockOption) *UserNotifQuery {
	if unq.driver.Dialect() == dialect.Postgres {
		unq.Unique(false)
	}
	unq.modifiers = append(unq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return unq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (unq *UserNotifQuery) Modify(modifiers ...func(s *sql.Selector)) *UserNotifSelect {
	unq.modifiers = append(unq.modifiers, modifiers...)
	return unq.Select()
}

// UserNotifGroupBy is the group-by builder for UserNotif entities.
type UserNotifGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ungb *UserNotifGroupBy) Aggregate(fns ...AggregateFunc) *UserNotifGroupBy {
	ungb.fns = append(ungb.fns, fns...)
	return ungb
}

// Scan applies the group-by query and scans the result into the given value.
func (ungb *UserNotifGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := ungb.path(ctx)
	if err != nil {
		return err
	}
	ungb.sql = query
	return ungb.sqlScan(ctx, v)
}

func (ungb *UserNotifGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range ungb.fields {
		if !usernotif.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := ungb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ungb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (ungb *UserNotifGroupBy) sqlQuery() *sql.Selector {
	selector := ungb.sql.Select()
	aggregation := make([]string, 0, len(ungb.fns))
	for _, fn := range ungb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(ungb.fields)+len(ungb.fns))
		for _, f := range ungb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(ungb.fields...)...)
}

// UserNotifSelect is the builder for selecting fields of UserNotif entities.
type UserNotifSelect struct {
	*UserNotifQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (uns *UserNotifSelect) Scan(ctx context.Context, v interface{}) error {
	if err := uns.prepareQuery(ctx); err != nil {
		return err
	}
	uns.sql = uns.UserNotifQuery.sqlQuery(ctx)
	return uns.sqlScan(ctx, v)
}

func (uns *UserNotifSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := uns.sql.Query()
	if err := uns.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (uns *UserNotifSelect) Modify(modifiers ...func(s *sql.Selector)) *UserNotifSelect {
	uns.modifiers = append(uns.modifiers, modifiers...)
	return uns
}
