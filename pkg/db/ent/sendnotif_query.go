// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/notif-middleware/pkg/db/ent/predicate"
	"github.com/NpoolPlatform/notif-middleware/pkg/db/ent/sendnotif"
	"github.com/google/uuid"
)

// SendNotifQuery is the builder for querying SendNotif entities.
type SendNotifQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.SendNotif
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SendNotifQuery builder.
func (snq *SendNotifQuery) Where(ps ...predicate.SendNotif) *SendNotifQuery {
	snq.predicates = append(snq.predicates, ps...)
	return snq
}

// Limit adds a limit step to the query.
func (snq *SendNotifQuery) Limit(limit int) *SendNotifQuery {
	snq.limit = &limit
	return snq
}

// Offset adds an offset step to the query.
func (snq *SendNotifQuery) Offset(offset int) *SendNotifQuery {
	snq.offset = &offset
	return snq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (snq *SendNotifQuery) Unique(unique bool) *SendNotifQuery {
	snq.unique = &unique
	return snq
}

// Order adds an order step to the query.
func (snq *SendNotifQuery) Order(o ...OrderFunc) *SendNotifQuery {
	snq.order = append(snq.order, o...)
	return snq
}

// First returns the first SendNotif entity from the query.
// Returns a *NotFoundError when no SendNotif was found.
func (snq *SendNotifQuery) First(ctx context.Context) (*SendNotif, error) {
	nodes, err := snq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{sendnotif.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (snq *SendNotifQuery) FirstX(ctx context.Context) *SendNotif {
	node, err := snq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SendNotif ID from the query.
// Returns a *NotFoundError when no SendNotif ID was found.
func (snq *SendNotifQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = snq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{sendnotif.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (snq *SendNotifQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := snq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SendNotif entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SendNotif entity is found.
// Returns a *NotFoundError when no SendNotif entities are found.
func (snq *SendNotifQuery) Only(ctx context.Context) (*SendNotif, error) {
	nodes, err := snq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{sendnotif.Label}
	default:
		return nil, &NotSingularError{sendnotif.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (snq *SendNotifQuery) OnlyX(ctx context.Context) *SendNotif {
	node, err := snq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SendNotif ID in the query.
// Returns a *NotSingularError when more than one SendNotif ID is found.
// Returns a *NotFoundError when no entities are found.
func (snq *SendNotifQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = snq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{sendnotif.Label}
	default:
		err = &NotSingularError{sendnotif.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (snq *SendNotifQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := snq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SendNotifs.
func (snq *SendNotifQuery) All(ctx context.Context) ([]*SendNotif, error) {
	if err := snq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return snq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (snq *SendNotifQuery) AllX(ctx context.Context) []*SendNotif {
	nodes, err := snq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SendNotif IDs.
func (snq *SendNotifQuery) IDs(ctx context.Context) ([]uuid.UUID, error) {
	var ids []uuid.UUID
	if err := snq.Select(sendnotif.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (snq *SendNotifQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := snq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (snq *SendNotifQuery) Count(ctx context.Context) (int, error) {
	if err := snq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return snq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (snq *SendNotifQuery) CountX(ctx context.Context) int {
	count, err := snq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (snq *SendNotifQuery) Exist(ctx context.Context) (bool, error) {
	if err := snq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return snq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (snq *SendNotifQuery) ExistX(ctx context.Context) bool {
	exist, err := snq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SendNotifQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (snq *SendNotifQuery) Clone() *SendNotifQuery {
	if snq == nil {
		return nil
	}
	return &SendNotifQuery{
		config:     snq.config,
		limit:      snq.limit,
		offset:     snq.offset,
		order:      append([]OrderFunc{}, snq.order...),
		predicates: append([]predicate.SendNotif{}, snq.predicates...),
		// clone intermediate query.
		sql:    snq.sql.Clone(),
		path:   snq.path,
		unique: snq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SendNotif.Query().
//		GroupBy(sendnotif.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (snq *SendNotifQuery) GroupBy(field string, fields ...string) *SendNotifGroupBy {
	grbuild := &SendNotifGroupBy{config: snq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := snq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return snq.sqlQuery(ctx), nil
	}
	grbuild.label = sendnotif.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.SendNotif.Query().
//		Select(sendnotif.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (snq *SendNotifQuery) Select(fields ...string) *SendNotifSelect {
	snq.fields = append(snq.fields, fields...)
	selbuild := &SendNotifSelect{SendNotifQuery: snq}
	selbuild.label = sendnotif.Label
	selbuild.flds, selbuild.scan = &snq.fields, selbuild.Scan
	return selbuild
}

func (snq *SendNotifQuery) prepareQuery(ctx context.Context) error {
	for _, f := range snq.fields {
		if !sendnotif.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if snq.path != nil {
		prev, err := snq.path(ctx)
		if err != nil {
			return err
		}
		snq.sql = prev
	}
	if sendnotif.Policy == nil {
		return errors.New("ent: uninitialized sendnotif.Policy (forgotten import ent/runtime?)")
	}
	if err := sendnotif.Policy.EvalQuery(ctx, snq); err != nil {
		return err
	}
	return nil
}

func (snq *SendNotifQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SendNotif, error) {
	var (
		nodes = []*SendNotif{}
		_spec = snq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*SendNotif).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &SendNotif{config: snq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(snq.modifiers) > 0 {
		_spec.Modifiers = snq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, snq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (snq *SendNotifQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := snq.querySpec()
	if len(snq.modifiers) > 0 {
		_spec.Modifiers = snq.modifiers
	}
	_spec.Node.Columns = snq.fields
	if len(snq.fields) > 0 {
		_spec.Unique = snq.unique != nil && *snq.unique
	}
	return sqlgraph.CountNodes(ctx, snq.driver, _spec)
}

func (snq *SendNotifQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := snq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (snq *SendNotifQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   sendnotif.Table,
			Columns: sendnotif.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUUID,
				Column: sendnotif.FieldID,
			},
		},
		From:   snq.sql,
		Unique: true,
	}
	if unique := snq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := snq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, sendnotif.FieldID)
		for i := range fields {
			if fields[i] != sendnotif.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := snq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := snq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := snq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := snq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (snq *SendNotifQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(snq.driver.Dialect())
	t1 := builder.Table(sendnotif.Table)
	columns := snq.fields
	if len(columns) == 0 {
		columns = sendnotif.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if snq.sql != nil {
		selector = snq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if snq.unique != nil && *snq.unique {
		selector.Distinct()
	}
	for _, m := range snq.modifiers {
		m(selector)
	}
	for _, p := range snq.predicates {
		p(selector)
	}
	for _, p := range snq.order {
		p(selector)
	}
	if offset := snq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := snq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (snq *SendNotifQuery) ForUpdate(opts ...sql.LockOption) *SendNotifQuery {
	if snq.driver.Dialect() == dialect.Postgres {
		snq.Unique(false)
	}
	snq.modifiers = append(snq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return snq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (snq *SendNotifQuery) ForShare(opts ...sql.LockOption) *SendNotifQuery {
	if snq.driver.Dialect() == dialect.Postgres {
		snq.Unique(false)
	}
	snq.modifiers = append(snq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return snq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (snq *SendNotifQuery) Modify(modifiers ...func(s *sql.Selector)) *SendNotifSelect {
	snq.modifiers = append(snq.modifiers, modifiers...)
	return snq.Select()
}

// SendNotifGroupBy is the group-by builder for SendNotif entities.
type SendNotifGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (sngb *SendNotifGroupBy) Aggregate(fns ...AggregateFunc) *SendNotifGroupBy {
	sngb.fns = append(sngb.fns, fns...)
	return sngb
}

// Scan applies the group-by query and scans the result into the given value.
func (sngb *SendNotifGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := sngb.path(ctx)
	if err != nil {
		return err
	}
	sngb.sql = query
	return sngb.sqlScan(ctx, v)
}

func (sngb *SendNotifGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range sngb.fields {
		if !sendnotif.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := sngb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sngb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (sngb *SendNotifGroupBy) sqlQuery() *sql.Selector {
	selector := sngb.sql.Select()
	aggregation := make([]string, 0, len(sngb.fns))
	for _, fn := range sngb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(sngb.fields)+len(sngb.fns))
		for _, f := range sngb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(sngb.fields...)...)
}

// SendNotifSelect is the builder for selecting fields of SendNotif entities.
type SendNotifSelect struct {
	*SendNotifQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (sns *SendNotifSelect) Scan(ctx context.Context, v interface{}) error {
	if err := sns.prepareQuery(ctx); err != nil {
		return err
	}
	sns.sql = sns.SendNotifQuery.sqlQuery(ctx)
	return sns.sqlScan(ctx, v)
}

func (sns *SendNotifSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := sns.sql.Query()
	if err := sns.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (sns *SendNotifSelect) Modify(modifiers ...func(s *sql.Selector)) *SendNotifSelect {
	sns.modifiers = append(sns.modifiers, modifiers...)
	return sns
}
