// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/notif-manager/pkg/db/ent/predicate"
	"github.com/NpoolPlatform/notif-manager/pkg/db/ent/userannouncement"
	"github.com/google/uuid"
)

// UserAnnouncementQuery is the builder for querying UserAnnouncement entities.
type UserAnnouncementQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.UserAnnouncement
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UserAnnouncementQuery builder.
func (uaq *UserAnnouncementQuery) Where(ps ...predicate.UserAnnouncement) *UserAnnouncementQuery {
	uaq.predicates = append(uaq.predicates, ps...)
	return uaq
}

// Limit adds a limit step to the query.
func (uaq *UserAnnouncementQuery) Limit(limit int) *UserAnnouncementQuery {
	uaq.limit = &limit
	return uaq
}

// Offset adds an offset step to the query.
func (uaq *UserAnnouncementQuery) Offset(offset int) *UserAnnouncementQuery {
	uaq.offset = &offset
	return uaq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (uaq *UserAnnouncementQuery) Unique(unique bool) *UserAnnouncementQuery {
	uaq.unique = &unique
	return uaq
}

// Order adds an order step to the query.
func (uaq *UserAnnouncementQuery) Order(o ...OrderFunc) *UserAnnouncementQuery {
	uaq.order = append(uaq.order, o...)
	return uaq
}

// First returns the first UserAnnouncement entity from the query.
// Returns a *NotFoundError when no UserAnnouncement was found.
func (uaq *UserAnnouncementQuery) First(ctx context.Context) (*UserAnnouncement, error) {
	nodes, err := uaq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{userannouncement.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (uaq *UserAnnouncementQuery) FirstX(ctx context.Context) *UserAnnouncement {
	node, err := uaq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UserAnnouncement ID from the query.
// Returns a *NotFoundError when no UserAnnouncement ID was found.
func (uaq *UserAnnouncementQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = uaq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{userannouncement.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (uaq *UserAnnouncementQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := uaq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UserAnnouncement entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UserAnnouncement entity is found.
// Returns a *NotFoundError when no UserAnnouncement entities are found.
func (uaq *UserAnnouncementQuery) Only(ctx context.Context) (*UserAnnouncement, error) {
	nodes, err := uaq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{userannouncement.Label}
	default:
		return nil, &NotSingularError{userannouncement.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (uaq *UserAnnouncementQuery) OnlyX(ctx context.Context) *UserAnnouncement {
	node, err := uaq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UserAnnouncement ID in the query.
// Returns a *NotSingularError when more than one UserAnnouncement ID is found.
// Returns a *NotFoundError when no entities are found.
func (uaq *UserAnnouncementQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = uaq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{userannouncement.Label}
	default:
		err = &NotSingularError{userannouncement.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (uaq *UserAnnouncementQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := uaq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UserAnnouncements.
func (uaq *UserAnnouncementQuery) All(ctx context.Context) ([]*UserAnnouncement, error) {
	if err := uaq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return uaq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (uaq *UserAnnouncementQuery) AllX(ctx context.Context) []*UserAnnouncement {
	nodes, err := uaq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UserAnnouncement IDs.
func (uaq *UserAnnouncementQuery) IDs(ctx context.Context) ([]uuid.UUID, error) {
	var ids []uuid.UUID
	if err := uaq.Select(userannouncement.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (uaq *UserAnnouncementQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := uaq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (uaq *UserAnnouncementQuery) Count(ctx context.Context) (int, error) {
	if err := uaq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return uaq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (uaq *UserAnnouncementQuery) CountX(ctx context.Context) int {
	count, err := uaq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (uaq *UserAnnouncementQuery) Exist(ctx context.Context) (bool, error) {
	if err := uaq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return uaq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (uaq *UserAnnouncementQuery) ExistX(ctx context.Context) bool {
	exist, err := uaq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UserAnnouncementQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (uaq *UserAnnouncementQuery) Clone() *UserAnnouncementQuery {
	if uaq == nil {
		return nil
	}
	return &UserAnnouncementQuery{
		config:     uaq.config,
		limit:      uaq.limit,
		offset:     uaq.offset,
		order:      append([]OrderFunc{}, uaq.order...),
		predicates: append([]predicate.UserAnnouncement{}, uaq.predicates...),
		// clone intermediate query.
		sql:    uaq.sql.Clone(),
		path:   uaq.path,
		unique: uaq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UserAnnouncement.Query().
//		GroupBy(userannouncement.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (uaq *UserAnnouncementQuery) GroupBy(field string, fields ...string) *UserAnnouncementGroupBy {
	grbuild := &UserAnnouncementGroupBy{config: uaq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := uaq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return uaq.sqlQuery(ctx), nil
	}
	grbuild.label = userannouncement.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.UserAnnouncement.Query().
//		Select(userannouncement.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (uaq *UserAnnouncementQuery) Select(fields ...string) *UserAnnouncementSelect {
	uaq.fields = append(uaq.fields, fields...)
	selbuild := &UserAnnouncementSelect{UserAnnouncementQuery: uaq}
	selbuild.label = userannouncement.Label
	selbuild.flds, selbuild.scan = &uaq.fields, selbuild.Scan
	return selbuild
}

func (uaq *UserAnnouncementQuery) prepareQuery(ctx context.Context) error {
	for _, f := range uaq.fields {
		if !userannouncement.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if uaq.path != nil {
		prev, err := uaq.path(ctx)
		if err != nil {
			return err
		}
		uaq.sql = prev
	}
	if userannouncement.Policy == nil {
		return errors.New("ent: uninitialized userannouncement.Policy (forgotten import ent/runtime?)")
	}
	if err := userannouncement.Policy.EvalQuery(ctx, uaq); err != nil {
		return err
	}
	return nil
}

func (uaq *UserAnnouncementQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UserAnnouncement, error) {
	var (
		nodes = []*UserAnnouncement{}
		_spec = uaq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*UserAnnouncement).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &UserAnnouncement{config: uaq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(uaq.modifiers) > 0 {
		_spec.Modifiers = uaq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, uaq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (uaq *UserAnnouncementQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := uaq.querySpec()
	if len(uaq.modifiers) > 0 {
		_spec.Modifiers = uaq.modifiers
	}
	_spec.Node.Columns = uaq.fields
	if len(uaq.fields) > 0 {
		_spec.Unique = uaq.unique != nil && *uaq.unique
	}
	return sqlgraph.CountNodes(ctx, uaq.driver, _spec)
}

func (uaq *UserAnnouncementQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := uaq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (uaq *UserAnnouncementQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   userannouncement.Table,
			Columns: userannouncement.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUUID,
				Column: userannouncement.FieldID,
			},
		},
		From:   uaq.sql,
		Unique: true,
	}
	if unique := uaq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := uaq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, userannouncement.FieldID)
		for i := range fields {
			if fields[i] != userannouncement.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := uaq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := uaq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := uaq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := uaq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (uaq *UserAnnouncementQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(uaq.driver.Dialect())
	t1 := builder.Table(userannouncement.Table)
	columns := uaq.fields
	if len(columns) == 0 {
		columns = userannouncement.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if uaq.sql != nil {
		selector = uaq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if uaq.unique != nil && *uaq.unique {
		selector.Distinct()
	}
	for _, m := range uaq.modifiers {
		m(selector)
	}
	for _, p := range uaq.predicates {
		p(selector)
	}
	for _, p := range uaq.order {
		p(selector)
	}
	if offset := uaq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := uaq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (uaq *UserAnnouncementQuery) ForUpdate(opts ...sql.LockOption) *UserAnnouncementQuery {
	if uaq.driver.Dialect() == dialect.Postgres {
		uaq.Unique(false)
	}
	uaq.modifiers = append(uaq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return uaq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (uaq *UserAnnouncementQuery) ForShare(opts ...sql.LockOption) *UserAnnouncementQuery {
	if uaq.driver.Dialect() == dialect.Postgres {
		uaq.Unique(false)
	}
	uaq.modifiers = append(uaq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return uaq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (uaq *UserAnnouncementQuery) Modify(modifiers ...func(s *sql.Selector)) *UserAnnouncementSelect {
	uaq.modifiers = append(uaq.modifiers, modifiers...)
	return uaq.Select()
}

// UserAnnouncementGroupBy is the group-by builder for UserAnnouncement entities.
type UserAnnouncementGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (uagb *UserAnnouncementGroupBy) Aggregate(fns ...AggregateFunc) *UserAnnouncementGroupBy {
	uagb.fns = append(uagb.fns, fns...)
	return uagb
}

// Scan applies the group-by query and scans the result into the given value.
func (uagb *UserAnnouncementGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := uagb.path(ctx)
	if err != nil {
		return err
	}
	uagb.sql = query
	return uagb.sqlScan(ctx, v)
}

func (uagb *UserAnnouncementGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range uagb.fields {
		if !userannouncement.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := uagb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := uagb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (uagb *UserAnnouncementGroupBy) sqlQuery() *sql.Selector {
	selector := uagb.sql.Select()
	aggregation := make([]string, 0, len(uagb.fns))
	for _, fn := range uagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(uagb.fields)+len(uagb.fns))
		for _, f := range uagb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(uagb.fields...)...)
}

// UserAnnouncementSelect is the builder for selecting fields of UserAnnouncement entities.
type UserAnnouncementSelect struct {
	*UserAnnouncementQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (uas *UserAnnouncementSelect) Scan(ctx context.Context, v interface{}) error {
	if err := uas.prepareQuery(ctx); err != nil {
		return err
	}
	uas.sql = uas.UserAnnouncementQuery.sqlQuery(ctx)
	return uas.sqlScan(ctx, v)
}

func (uas *UserAnnouncementSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := uas.sql.Query()
	if err := uas.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (uas *UserAnnouncementSelect) Modify(modifiers ...func(s *sql.Selector)) *UserAnnouncementSelect {
	uas.modifiers = append(uas.modifiers, modifiers...)
	return uas
}
