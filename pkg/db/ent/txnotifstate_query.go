// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/notif-manager/pkg/db/ent/predicate"
	"github.com/NpoolPlatform/notif-manager/pkg/db/ent/txnotifstate"
	"github.com/google/uuid"
)

// TxNotifStateQuery is the builder for querying TxNotifState entities.
type TxNotifStateQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.TxNotifState
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TxNotifStateQuery builder.
func (tnsq *TxNotifStateQuery) Where(ps ...predicate.TxNotifState) *TxNotifStateQuery {
	tnsq.predicates = append(tnsq.predicates, ps...)
	return tnsq
}

// Limit adds a limit step to the query.
func (tnsq *TxNotifStateQuery) Limit(limit int) *TxNotifStateQuery {
	tnsq.limit = &limit
	return tnsq
}

// Offset adds an offset step to the query.
func (tnsq *TxNotifStateQuery) Offset(offset int) *TxNotifStateQuery {
	tnsq.offset = &offset
	return tnsq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (tnsq *TxNotifStateQuery) Unique(unique bool) *TxNotifStateQuery {
	tnsq.unique = &unique
	return tnsq
}

// Order adds an order step to the query.
func (tnsq *TxNotifStateQuery) Order(o ...OrderFunc) *TxNotifStateQuery {
	tnsq.order = append(tnsq.order, o...)
	return tnsq
}

// First returns the first TxNotifState entity from the query.
// Returns a *NotFoundError when no TxNotifState was found.
func (tnsq *TxNotifStateQuery) First(ctx context.Context) (*TxNotifState, error) {
	nodes, err := tnsq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{txnotifstate.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (tnsq *TxNotifStateQuery) FirstX(ctx context.Context) *TxNotifState {
	node, err := tnsq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TxNotifState ID from the query.
// Returns a *NotFoundError when no TxNotifState ID was found.
func (tnsq *TxNotifStateQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = tnsq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{txnotifstate.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (tnsq *TxNotifStateQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := tnsq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TxNotifState entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TxNotifState entity is found.
// Returns a *NotFoundError when no TxNotifState entities are found.
func (tnsq *TxNotifStateQuery) Only(ctx context.Context) (*TxNotifState, error) {
	nodes, err := tnsq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{txnotifstate.Label}
	default:
		return nil, &NotSingularError{txnotifstate.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (tnsq *TxNotifStateQuery) OnlyX(ctx context.Context) *TxNotifState {
	node, err := tnsq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TxNotifState ID in the query.
// Returns a *NotSingularError when more than one TxNotifState ID is found.
// Returns a *NotFoundError when no entities are found.
func (tnsq *TxNotifStateQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = tnsq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{txnotifstate.Label}
	default:
		err = &NotSingularError{txnotifstate.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (tnsq *TxNotifStateQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := tnsq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TxNotifStates.
func (tnsq *TxNotifStateQuery) All(ctx context.Context) ([]*TxNotifState, error) {
	if err := tnsq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return tnsq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (tnsq *TxNotifStateQuery) AllX(ctx context.Context) []*TxNotifState {
	nodes, err := tnsq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TxNotifState IDs.
func (tnsq *TxNotifStateQuery) IDs(ctx context.Context) ([]uuid.UUID, error) {
	var ids []uuid.UUID
	if err := tnsq.Select(txnotifstate.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (tnsq *TxNotifStateQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := tnsq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (tnsq *TxNotifStateQuery) Count(ctx context.Context) (int, error) {
	if err := tnsq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return tnsq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (tnsq *TxNotifStateQuery) CountX(ctx context.Context) int {
	count, err := tnsq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (tnsq *TxNotifStateQuery) Exist(ctx context.Context) (bool, error) {
	if err := tnsq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return tnsq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (tnsq *TxNotifStateQuery) ExistX(ctx context.Context) bool {
	exist, err := tnsq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TxNotifStateQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (tnsq *TxNotifStateQuery) Clone() *TxNotifStateQuery {
	if tnsq == nil {
		return nil
	}
	return &TxNotifStateQuery{
		config:     tnsq.config,
		limit:      tnsq.limit,
		offset:     tnsq.offset,
		order:      append([]OrderFunc{}, tnsq.order...),
		predicates: append([]predicate.TxNotifState{}, tnsq.predicates...),
		// clone intermediate query.
		sql:    tnsq.sql.Clone(),
		path:   tnsq.path,
		unique: tnsq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TxNotifState.Query().
//		GroupBy(txnotifstate.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (tnsq *TxNotifStateQuery) GroupBy(field string, fields ...string) *TxNotifStateGroupBy {
	grbuild := &TxNotifStateGroupBy{config: tnsq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := tnsq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return tnsq.sqlQuery(ctx), nil
	}
	grbuild.label = txnotifstate.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.TxNotifState.Query().
//		Select(txnotifstate.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (tnsq *TxNotifStateQuery) Select(fields ...string) *TxNotifStateSelect {
	tnsq.fields = append(tnsq.fields, fields...)
	selbuild := &TxNotifStateSelect{TxNotifStateQuery: tnsq}
	selbuild.label = txnotifstate.Label
	selbuild.flds, selbuild.scan = &tnsq.fields, selbuild.Scan
	return selbuild
}

func (tnsq *TxNotifStateQuery) prepareQuery(ctx context.Context) error {
	for _, f := range tnsq.fields {
		if !txnotifstate.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if tnsq.path != nil {
		prev, err := tnsq.path(ctx)
		if err != nil {
			return err
		}
		tnsq.sql = prev
	}
	if txnotifstate.Policy == nil {
		return errors.New("ent: uninitialized txnotifstate.Policy (forgotten import ent/runtime?)")
	}
	if err := txnotifstate.Policy.EvalQuery(ctx, tnsq); err != nil {
		return err
	}
	return nil
}

func (tnsq *TxNotifStateQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TxNotifState, error) {
	var (
		nodes = []*TxNotifState{}
		_spec = tnsq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*TxNotifState).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &TxNotifState{config: tnsq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(tnsq.modifiers) > 0 {
		_spec.Modifiers = tnsq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, tnsq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (tnsq *TxNotifStateQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := tnsq.querySpec()
	if len(tnsq.modifiers) > 0 {
		_spec.Modifiers = tnsq.modifiers
	}
	_spec.Node.Columns = tnsq.fields
	if len(tnsq.fields) > 0 {
		_spec.Unique = tnsq.unique != nil && *tnsq.unique
	}
	return sqlgraph.CountNodes(ctx, tnsq.driver, _spec)
}

func (tnsq *TxNotifStateQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := tnsq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (tnsq *TxNotifStateQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   txnotifstate.Table,
			Columns: txnotifstate.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUUID,
				Column: txnotifstate.FieldID,
			},
		},
		From:   tnsq.sql,
		Unique: true,
	}
	if unique := tnsq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := tnsq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, txnotifstate.FieldID)
		for i := range fields {
			if fields[i] != txnotifstate.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := tnsq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := tnsq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := tnsq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := tnsq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (tnsq *TxNotifStateQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(tnsq.driver.Dialect())
	t1 := builder.Table(txnotifstate.Table)
	columns := tnsq.fields
	if len(columns) == 0 {
		columns = txnotifstate.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if tnsq.sql != nil {
		selector = tnsq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if tnsq.unique != nil && *tnsq.unique {
		selector.Distinct()
	}
	for _, m := range tnsq.modifiers {
		m(selector)
	}
	for _, p := range tnsq.predicates {
		p(selector)
	}
	for _, p := range tnsq.order {
		p(selector)
	}
	if offset := tnsq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := tnsq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (tnsq *TxNotifStateQuery) ForUpdate(opts ...sql.LockOption) *TxNotifStateQuery {
	if tnsq.driver.Dialect() == dialect.Postgres {
		tnsq.Unique(false)
	}
	tnsq.modifiers = append(tnsq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return tnsq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (tnsq *TxNotifStateQuery) ForShare(opts ...sql.LockOption) *TxNotifStateQuery {
	if tnsq.driver.Dialect() == dialect.Postgres {
		tnsq.Unique(false)
	}
	tnsq.modifiers = append(tnsq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return tnsq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (tnsq *TxNotifStateQuery) Modify(modifiers ...func(s *sql.Selector)) *TxNotifStateSelect {
	tnsq.modifiers = append(tnsq.modifiers, modifiers...)
	return tnsq.Select()
}

// TxNotifStateGroupBy is the group-by builder for TxNotifState entities.
type TxNotifStateGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (tnsgb *TxNotifStateGroupBy) Aggregate(fns ...AggregateFunc) *TxNotifStateGroupBy {
	tnsgb.fns = append(tnsgb.fns, fns...)
	return tnsgb
}

// Scan applies the group-by query and scans the result into the given value.
func (tnsgb *TxNotifStateGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := tnsgb.path(ctx)
	if err != nil {
		return err
	}
	tnsgb.sql = query
	return tnsgb.sqlScan(ctx, v)
}

func (tnsgb *TxNotifStateGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range tnsgb.fields {
		if !txnotifstate.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := tnsgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tnsgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (tnsgb *TxNotifStateGroupBy) sqlQuery() *sql.Selector {
	selector := tnsgb.sql.Select()
	aggregation := make([]string, 0, len(tnsgb.fns))
	for _, fn := range tnsgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(tnsgb.fields)+len(tnsgb.fns))
		for _, f := range tnsgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(tnsgb.fields...)...)
}

// TxNotifStateSelect is the builder for selecting fields of TxNotifState entities.
type TxNotifStateSelect struct {
	*TxNotifStateQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (tnss *TxNotifStateSelect) Scan(ctx context.Context, v interface{}) error {
	if err := tnss.prepareQuery(ctx); err != nil {
		return err
	}
	tnss.sql = tnss.TxNotifStateQuery.sqlQuery(ctx)
	return tnss.sqlScan(ctx, v)
}

func (tnss *TxNotifStateSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := tnss.sql.Query()
	if err := tnss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (tnss *TxNotifStateSelect) Modify(modifiers ...func(s *sql.Selector)) *TxNotifStateSelect {
	tnss.modifiers = append(tnss.modifiers, modifiers...)
	return tnss
}
