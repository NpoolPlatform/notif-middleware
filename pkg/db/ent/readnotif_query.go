// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/notif-middleware/pkg/db/ent/predicate"
	"github.com/NpoolPlatform/notif-middleware/pkg/db/ent/readnotif"
	"github.com/google/uuid"
)

// ReadNotifQuery is the builder for querying ReadNotif entities.
type ReadNotifQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.ReadNotif
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ReadNotifQuery builder.
func (rnq *ReadNotifQuery) Where(ps ...predicate.ReadNotif) *ReadNotifQuery {
	rnq.predicates = append(rnq.predicates, ps...)
	return rnq
}

// Limit adds a limit step to the query.
func (rnq *ReadNotifQuery) Limit(limit int) *ReadNotifQuery {
	rnq.limit = &limit
	return rnq
}

// Offset adds an offset step to the query.
func (rnq *ReadNotifQuery) Offset(offset int) *ReadNotifQuery {
	rnq.offset = &offset
	return rnq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (rnq *ReadNotifQuery) Unique(unique bool) *ReadNotifQuery {
	rnq.unique = &unique
	return rnq
}

// Order adds an order step to the query.
func (rnq *ReadNotifQuery) Order(o ...OrderFunc) *ReadNotifQuery {
	rnq.order = append(rnq.order, o...)
	return rnq
}

// First returns the first ReadNotif entity from the query.
// Returns a *NotFoundError when no ReadNotif was found.
func (rnq *ReadNotifQuery) First(ctx context.Context) (*ReadNotif, error) {
	nodes, err := rnq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{readnotif.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (rnq *ReadNotifQuery) FirstX(ctx context.Context) *ReadNotif {
	node, err := rnq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ReadNotif ID from the query.
// Returns a *NotFoundError when no ReadNotif ID was found.
func (rnq *ReadNotifQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = rnq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{readnotif.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (rnq *ReadNotifQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := rnq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ReadNotif entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ReadNotif entity is found.
// Returns a *NotFoundError when no ReadNotif entities are found.
func (rnq *ReadNotifQuery) Only(ctx context.Context) (*ReadNotif, error) {
	nodes, err := rnq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{readnotif.Label}
	default:
		return nil, &NotSingularError{readnotif.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (rnq *ReadNotifQuery) OnlyX(ctx context.Context) *ReadNotif {
	node, err := rnq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ReadNotif ID in the query.
// Returns a *NotSingularError when more than one ReadNotif ID is found.
// Returns a *NotFoundError when no entities are found.
func (rnq *ReadNotifQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = rnq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{readnotif.Label}
	default:
		err = &NotSingularError{readnotif.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (rnq *ReadNotifQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := rnq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ReadNotifs.
func (rnq *ReadNotifQuery) All(ctx context.Context) ([]*ReadNotif, error) {
	if err := rnq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return rnq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (rnq *ReadNotifQuery) AllX(ctx context.Context) []*ReadNotif {
	nodes, err := rnq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ReadNotif IDs.
func (rnq *ReadNotifQuery) IDs(ctx context.Context) ([]uuid.UUID, error) {
	var ids []uuid.UUID
	if err := rnq.Select(readnotif.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (rnq *ReadNotifQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := rnq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (rnq *ReadNotifQuery) Count(ctx context.Context) (int, error) {
	if err := rnq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return rnq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (rnq *ReadNotifQuery) CountX(ctx context.Context) int {
	count, err := rnq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (rnq *ReadNotifQuery) Exist(ctx context.Context) (bool, error) {
	if err := rnq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return rnq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (rnq *ReadNotifQuery) ExistX(ctx context.Context) bool {
	exist, err := rnq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ReadNotifQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (rnq *ReadNotifQuery) Clone() *ReadNotifQuery {
	if rnq == nil {
		return nil
	}
	return &ReadNotifQuery{
		config:     rnq.config,
		limit:      rnq.limit,
		offset:     rnq.offset,
		order:      append([]OrderFunc{}, rnq.order...),
		predicates: append([]predicate.ReadNotif{}, rnq.predicates...),
		// clone intermediate query.
		sql:    rnq.sql.Clone(),
		path:   rnq.path,
		unique: rnq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ReadNotif.Query().
//		GroupBy(readnotif.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (rnq *ReadNotifQuery) GroupBy(field string, fields ...string) *ReadNotifGroupBy {
	grbuild := &ReadNotifGroupBy{config: rnq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := rnq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return rnq.sqlQuery(ctx), nil
	}
	grbuild.label = readnotif.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.ReadNotif.Query().
//		Select(readnotif.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (rnq *ReadNotifQuery) Select(fields ...string) *ReadNotifSelect {
	rnq.fields = append(rnq.fields, fields...)
	selbuild := &ReadNotifSelect{ReadNotifQuery: rnq}
	selbuild.label = readnotif.Label
	selbuild.flds, selbuild.scan = &rnq.fields, selbuild.Scan
	return selbuild
}

func (rnq *ReadNotifQuery) prepareQuery(ctx context.Context) error {
	for _, f := range rnq.fields {
		if !readnotif.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if rnq.path != nil {
		prev, err := rnq.path(ctx)
		if err != nil {
			return err
		}
		rnq.sql = prev
	}
	if readnotif.Policy == nil {
		return errors.New("ent: uninitialized readnotif.Policy (forgotten import ent/runtime?)")
	}
	if err := readnotif.Policy.EvalQuery(ctx, rnq); err != nil {
		return err
	}
	return nil
}

func (rnq *ReadNotifQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ReadNotif, error) {
	var (
		nodes = []*ReadNotif{}
		_spec = rnq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*ReadNotif).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &ReadNotif{config: rnq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(rnq.modifiers) > 0 {
		_spec.Modifiers = rnq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, rnq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (rnq *ReadNotifQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := rnq.querySpec()
	if len(rnq.modifiers) > 0 {
		_spec.Modifiers = rnq.modifiers
	}
	_spec.Node.Columns = rnq.fields
	if len(rnq.fields) > 0 {
		_spec.Unique = rnq.unique != nil && *rnq.unique
	}
	return sqlgraph.CountNodes(ctx, rnq.driver, _spec)
}

func (rnq *ReadNotifQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := rnq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (rnq *ReadNotifQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   readnotif.Table,
			Columns: readnotif.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUUID,
				Column: readnotif.FieldID,
			},
		},
		From:   rnq.sql,
		Unique: true,
	}
	if unique := rnq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := rnq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, readnotif.FieldID)
		for i := range fields {
			if fields[i] != readnotif.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := rnq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := rnq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := rnq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := rnq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (rnq *ReadNotifQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(rnq.driver.Dialect())
	t1 := builder.Table(readnotif.Table)
	columns := rnq.fields
	if len(columns) == 0 {
		columns = readnotif.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if rnq.sql != nil {
		selector = rnq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if rnq.unique != nil && *rnq.unique {
		selector.Distinct()
	}
	for _, m := range rnq.modifiers {
		m(selector)
	}
	for _, p := range rnq.predicates {
		p(selector)
	}
	for _, p := range rnq.order {
		p(selector)
	}
	if offset := rnq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := rnq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (rnq *ReadNotifQuery) ForUpdate(opts ...sql.LockOption) *ReadNotifQuery {
	if rnq.driver.Dialect() == dialect.Postgres {
		rnq.Unique(false)
	}
	rnq.modifiers = append(rnq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return rnq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (rnq *ReadNotifQuery) ForShare(opts ...sql.LockOption) *ReadNotifQuery {
	if rnq.driver.Dialect() == dialect.Postgres {
		rnq.Unique(false)
	}
	rnq.modifiers = append(rnq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return rnq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (rnq *ReadNotifQuery) Modify(modifiers ...func(s *sql.Selector)) *ReadNotifSelect {
	rnq.modifiers = append(rnq.modifiers, modifiers...)
	return rnq.Select()
}

// ReadNotifGroupBy is the group-by builder for ReadNotif entities.
type ReadNotifGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rngb *ReadNotifGroupBy) Aggregate(fns ...AggregateFunc) *ReadNotifGroupBy {
	rngb.fns = append(rngb.fns, fns...)
	return rngb
}

// Scan applies the group-by query and scans the result into the given value.
func (rngb *ReadNotifGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := rngb.path(ctx)
	if err != nil {
		return err
	}
	rngb.sql = query
	return rngb.sqlScan(ctx, v)
}

func (rngb *ReadNotifGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range rngb.fields {
		if !readnotif.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := rngb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rngb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (rngb *ReadNotifGroupBy) sqlQuery() *sql.Selector {
	selector := rngb.sql.Select()
	aggregation := make([]string, 0, len(rngb.fns))
	for _, fn := range rngb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(rngb.fields)+len(rngb.fns))
		for _, f := range rngb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(rngb.fields...)...)
}

// ReadNotifSelect is the builder for selecting fields of ReadNotif entities.
type ReadNotifSelect struct {
	*ReadNotifQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (rns *ReadNotifSelect) Scan(ctx context.Context, v interface{}) error {
	if err := rns.prepareQuery(ctx); err != nil {
		return err
	}
	rns.sql = rns.ReadNotifQuery.sqlQuery(ctx)
	return rns.sqlScan(ctx, v)
}

func (rns *ReadNotifSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := rns.sql.Query()
	if err := rns.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (rns *ReadNotifSelect) Modify(modifiers ...func(s *sql.Selector)) *ReadNotifSelect {
	rns.modifiers = append(rns.modifiers, modifiers...)
	return rns
}
